// @cascade/core — domain model, the 11 column-type registry (validation /
// coercion / CSV / filter operators), and the clipboard parser.
// Fleshed out in the "core contracts" stage; placeholder export for now.
export const CORE_VERSION = '0.1.0'
