// @cascade/data — the API contract (interface) plus a MockApi implementation
// backed by an in-memory store persisted to localStorage, with seed data.
// Fleshed out in the "core contracts" stage; placeholder export for now.
export const DATA_VERSION = '0.1.0'
