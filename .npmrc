# Flat node_modules — maximises compatibility for Next.js + Glide Data Grid +
# Radix in a pnpm workspace and avoids peer-dependency resolution friction.
node-linker=hoisted
strict-peer-dependencies=false
auto-install-peers=true
